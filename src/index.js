import React from 'react';
import ReactDOM from 'react-dom/client';
import NavanaaEstate from '../NavanaaEstate';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
      <NavanaaEstate />
        </React.StrictMode>
        );
